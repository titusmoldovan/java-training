package clean.code.chess.solution.pieces;

/**
 * This enumeration defines the type of movement
 * MOVE = a simple move
 * CAPTURE = a move by which an opponent's piece is captured
 */
public enum MovementType {

    MOVE, CAPTURE
}
